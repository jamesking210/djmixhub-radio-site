window.DJMIXHUB_CONFIG = Object.freeze({
  siteUrl: "${PUBLIC_SITE_URL}",
  siteName: "${PUBLIC_SITE_NAME}",
  siteTagline: "${PUBLIC_SITE_TAGLINE}",
  radioBaseUrl: "${PUBLIC_RADIO_BASE_URL}",
  stationShortcode: "${PUBLIC_STATION_SHORTCODE}",
  stationName: "${PUBLIC_STATION_NAME}",
  nowPlayingUrl: "${PUBLIC_NOW_PLAYING_URL}",
  streamUrl: "${PUBLIC_STREAM_URL}",
  mainRepoUrl: "${PUBLIC_MAIN_REPO_URL}",
  azuracastRepoUrl: "${PUBLIC_AZURACAST_REPO_URL}",
  githubUrl: "${PUBLIC_GITHUB_URL}",
  contactEmail: "${PUBLIC_CONTACT_EMAIL}"
});
